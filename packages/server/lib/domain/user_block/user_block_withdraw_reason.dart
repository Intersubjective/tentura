/// System withdraw reason recorded on help offers withdrawn during a direct block.
///
/// Not user-selectable — [HelpOfferCase] validates interactive reasons separately.
const kBlockWithdrawReason = 'blocked';
