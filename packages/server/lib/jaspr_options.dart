import 'package:jaspr/server.dart';

/// Default server options for Jaspr.
/// Compatibility file: jaspr_builder 0.22+ generates this only for lib/*.server.dart
/// entrypoints; this package uses bin/tentura.dart, so we provide defaults here.
ServerOptions get defaultJasprOptions => const ServerOptions();
