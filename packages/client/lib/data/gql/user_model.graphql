# import '/data/gql/image_model.graphql'

fragment UserModel on user {
  id
  title
  description
  my_vote
  image {
    ...ImageModel
  }
  scores {
    src_score
    dst_score
  }
  user_presence {
    last_seen_at
    status
  }
}
