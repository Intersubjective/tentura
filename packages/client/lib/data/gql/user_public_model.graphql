# import '/data/gql/image_model.graphql'

fragment UserPublicModel on user {
  id
  displayName
  handle
  description
  my_vote
  is_mutual_friend
  image {
    ...ImageModel
  }
  scores {
    src_score
    dst_score
  }
  user_presence {
    last_seen_at
    status
  }
}
