query BeaconLineageForwardSuggestions($id: String!) {
  beaconLineageForwardSuggestions(id: $id) {
    sourceBeaconId
    rootBeaconId
    suggestedNote
    suggestions {
      userId
      group
      reasonCode
      reasonArg
      autoSelect
    }
  }
}
