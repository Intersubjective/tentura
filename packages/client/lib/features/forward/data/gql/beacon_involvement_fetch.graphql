# import '/data/gql/beacon_model.graphql'

query BeaconInvolvementFetch($beaconId: String!) {
  beacon_by_pk(id: $beaconId) {
    ...BeaconModel
    forward_edges {
      recipient_id
    }
    commitments {
      user_id
      status
    }
    rejected_user_ids
  }
}
