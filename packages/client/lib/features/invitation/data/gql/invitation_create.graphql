mutation InvitationCreate($addresseeName: String!, $beaconId: String) {
  invitationCreate(addresseeName: $addresseeName, beaconId: $beaconId) {
    id
    beacon_id
    invited_id
    addressee_name
    created_at
    updated_at
  }
}
