query EvaluationParticipants($id: String!) {
  evaluationParticipants(id: $id) {
    userId
    displayName
    imageId
    role
    contributionSummary
    causalHint
    value
    reasonTags
    note
    promptVariant
  }
}
