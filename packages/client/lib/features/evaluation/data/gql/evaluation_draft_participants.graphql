query EvaluationDraftParticipants($id: String!) {
  evaluationDraftParticipants(id: $id) {
    userId
    title
    imageId
    role
    contributionSummary
    causalHint
    value
    reasonTags
    note
    promptVariant
  }
}
