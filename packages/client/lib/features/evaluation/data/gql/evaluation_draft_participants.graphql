query EvaluationDraftParticipants($id: String!) {
  evaluationDraftParticipants(id: $id) {
    userId
    displayName
    imageId
    role
    contributionSummary
    causalHint
    value
    reasonTags
    note
    promptVariant
  }
}
