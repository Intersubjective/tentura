mutation BeaconClose($id: String!, $expectedRequiresReviewWindow: Boolean!) {
  beaconClose(id: $id, expectedRequiresReviewWindow: $expectedRequiresReviewWindow) {
    id
    status
    closesAt
  }
}
