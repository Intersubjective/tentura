query ReviewWindowStatus($id: String!) {
  reviewWindowStatus(id: $id) {
    beaconId
    hasWindow
    beaconTitle
    openedAt
    closesAt
    windowComplete
    userReviewStatus
    reviewedCount
    totalCount
  }
}
