query PersonTopCapabilitiesBatch(
  $subjectUserIds: [String!]!
  $limit: Int
  $prioritizeSlugs: [String!]
) {
  personTopCapabilitiesBatch(
    subjectUserIds: $subjectUserIds
    limit: $limit
    prioritizeSlugs: $prioritizeSlugs
  ) {
    subjectId
    slugs
  }
}
