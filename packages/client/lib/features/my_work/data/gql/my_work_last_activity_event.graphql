query MyWorkLastActivityEvent($beaconIds: [String!]!) {
  myWorkLastActivityEvent(beaconIds: $beaconIds) {
    beaconId
    id
    type
    actorId
    actorTitle
    actorImageId
    createdAt
    diffJson
  }
}
