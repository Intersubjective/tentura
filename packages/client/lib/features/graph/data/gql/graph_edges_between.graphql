query GraphEdgesBetween($node_ids: _text!, $positive_only: Boolean = true) {
  graph_edges_between(args: { node_ids: $node_ids, positive_only: $positive_only }) {
    src
    dst
    src_score
    dst_score
    src_total_neighbor_count
    dst_total_neighbor_count
  }
}
