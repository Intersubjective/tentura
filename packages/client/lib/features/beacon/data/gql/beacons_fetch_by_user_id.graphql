# import '/data/gql/beacon_model.graphql'

query BeaconsFetchByUserId(
  $user_id: String!,
  $active_states: [smallint!]!,
  $offset: Int = 0,
  $limit: Int = 5
) {
  beacon(
    where: {
      _and: {
        user_id: {_eq: $user_id},
        state: {_in: $active_states}
      }
    },
    order_by: {created_at: desc},
    offset: $offset,
    limit: $limit
  ) {
    ...BeaconModel
  }
}
