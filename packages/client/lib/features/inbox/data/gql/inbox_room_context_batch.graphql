query InboxRoomContextBatch($beaconIds: [String!]!) {
  InboxRoomContextBatch(beaconIds: $beaconIds) {
    beaconId
    isRoomMember
    currentLine
    lastRoomMeaningfulChange
    nextMoveText
    roomUnreadCount
    lastSeenAt
    openBlockerTitle
    publicFactSnippet
  }
}
