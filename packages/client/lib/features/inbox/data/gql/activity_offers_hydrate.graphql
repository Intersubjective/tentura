# import 'inbox_item_fields.graphql'

query ActivityOffersHydrate($userId: String!, $beaconIds: [String!]!) {
  inbox_item(
    where: {
      beacon_id: {_in: $beaconIds}
      status: {_eq: 0}
      beacon: {can_read_content: {_eq: true}}
    }
  ) {
    ...InboxItemFields
  }
}
