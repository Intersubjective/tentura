query BlockPreview($objectId: String!, $cascadeMode: Int) {
  blockPreview(objectId: $objectId, cascadeMode: $cascadeMode) {
    cascadeCandidateCount
    cascadeCapped
    openCommitmentCount
    willWithdrawEdge
  }
}
