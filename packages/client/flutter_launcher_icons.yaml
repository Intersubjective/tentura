flutter_launcher_icons:
  ios: true
  android: true
  remove_alpha_ios: true
  image_path: "images/logo1024.png"

  # Web PWA icons are `web/tentura-icon-*.png` (cache-busted names). Do not
  # regenerate Flutter's default `web/icons/Icon-*.png` — iOS pins that path.
  web:
    generate: false
