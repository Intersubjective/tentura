name: web_socket_client
description: >
  Tentura-patched web_socket_client 0.2.1 (Completer race + channel onError).
  Remove when https://github.com/felangel/web_socket_client/pull/87 is
  merged+released and upstream also handles stream onError / Object catch.
repository: https://github.com/felangel/web_socket_client
version: 0.2.1+tentura.1
topics: [web-socket, web-socket-client]
funding: [https://github.com/sponsors/felangel]

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  meta: ^1.0.0
  web: ">=0.5.0 <2.0.0"
  web_socket_channel: ">=2.4.5 <4.0.0"

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^7.0.0
