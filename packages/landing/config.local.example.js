// Local dev overlay — copy to config.local.js or run ./scripts/sync-landing-local-config.sh
window.TENTURA = Object.assign(window.TENTURA || {}, {
  appBase: 'https://app.dev.lvh.me:9443/',
  googleEnabled: true,
});
