# Default when you run `docker compose` with no -f: local dev infrastructure only.
# Tentura API runs on the host (see compose.dev.yaml header).
#
# Production (isolated networks, Tentura in Docker): use compose.prod.yaml
#   docker compose -f compose.prod.yaml up -d

include:
  - compose.dev.yaml
