enum WebSocketState {
  connected,
  disconnected,
}

enum UserPresenceStatus {
  unknown,
  online,
  offline,
  inactive,
}

enum ComplaintType {
  unknown,
  violatesCsaePolicy,
  violatesPlatformRules,
}
